$numero = 1;         
print $numero;      #Imprime en la salida stádar el escalar

@numeros = (1, 2, 3, 4);
print $numeros[2];   #Imprime un elemento

%numeros = (
    numero => 1,
    palabra => 'word',
);
print $numeros{palabra};     #Imprime 'word'
